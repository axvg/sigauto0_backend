coverage:
  status:
    project:
      default:
        informational: true
        threshold: 5%
    patch:
      default:
        informational: true

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false
  require_head: true
  hide_project_coverage: false